# The scanner itself (scan.py, backtest.py, dashboard.py) uses only the Python
# standard library (urllib, json, csv) — no third-party packages required.
#
# The webhook backend (server.py) needs these:
fastapi>=0.115
uvicorn[standard]>=0.30
#
# The async Unusual Whales validator (uw_validator.py) needs:
httpx>=0.27
#
# Python 3.10+ is required (uses X | None type syntax).
